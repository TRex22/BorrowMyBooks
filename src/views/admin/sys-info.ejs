<!DOCTYPE html>
<html>

<%- include('../partials/header') %>

<body>
    <%- include('../partials/navbar') %>
        <div class="bs-component">
            <h1 class="center-align">System Information</h1>

            <div class="row">

	            <div class="col-md-3">
	                <span class="glyphicon glyphicon-stats stats-glyph" aria-hidden="true"></span>
	            </div>

	            <div class="col-md-5">
	                <h3>System Summary</h3>
	                <ul>
	                	<li>Up Time: <strong><%= sysinfo.upTime %> seconds</strong></li>
	                    <li title="<%= sysinfo.startTime %>">Start Time: <strong><%= site.moment(sysinfo.startTime).format('LLL'); %></strong></li>
	                    <li>Node Version: <strong><%= sysinfo.nodeVersion %></strong></li>
	                    <li>Node PID: <strong><%= sysinfo.pid %></strong></li>
	                    <li>Load Average: <strong><% if (sysinfo.loadAvg) {%> 
	                                    <%= sysinfo.loadAvg[0]  %> 
	                                      <% for(var i=1; i<sysinfo.loadAvg.length; i++) {%>
	                                         , <%= sysinfo.loadAvg[0]  %> 
	                                     <% } %> 
	                                   <%} %> </strong></li>
	                    <li>Free Memory: <strong><%= sysinfo.freeMemory %></strong></li>
	                    <li>Used Memory: <strong><%= sysinfo.memoryUsage %></strong></li>
						<li>Environment: <strong><%= sysinfo.env %></strong></li>
	                    <li>Hostname: <strong><%= sysinfo.hostname %></strong></li>
	                    <li>Server Ip: <strong><% if (sysinfo.serverIp) {%> 
	                                    <%= sysinfo.serverIp[0]  %> 
	                                      <% for(var i=1; i<sysinfo.serverIp.length; i++) {%>
	                                       , <%= sysinfo.serverIp[0]  %>
	                                     <% } %> 
	                                   <%} %> </strong></li>                  
	                </ul>

	                <br>

	            </div>
			</div>
        </div>
        <%- include('../partials/footer') %>
</body>
</html>
